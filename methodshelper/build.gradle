apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'

android {
    compileSdkVersion 28



    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
}

ext {
    bintrayRepo = 'Helper-Methods' //the maven repo name (created on bintray)
    bintrayName = 'commonmethod' //the name you want to give at your project on bintray
    orgName = 'ashrafcommon' //your user name

    publishedGroupId = 'com.ashraf' //aaaa : the librairy group
    artifact = 'commonmethod' //BBBB : the library name
    libraryVersion = "0.0.1" //the librairy version

    //the library will be : aaaa:BBBB:version

    libraryName = 'MethodHelper'
    libraryDescription = 'This library gives you support of commonly used methods in android in bundle'

    siteUrl = 'https://github.com/ashrafcommon/Common-Metods'
    gitUrl = 'https://github.com/ashrafcommon/Common-Metods.git'

    developerId = 'ashrafcommon'
    developerName = 'Ashraf Iqubal'
    developerEmail = 'ashrafcommon@gmail.com'

    licenseName = 'The Apache Software License, Version 2.0'
    licenseUrl = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
    allLicenses = ["Apache-2.0"]
}

apply from: rootProject.file('gradle/install-v1.gradle')
apply from: rootProject.file('gradle/bintray-android-v1.gradle')
